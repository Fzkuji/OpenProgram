"""Reusable self-programming Workflow projects.

Each direct child is an independent Git-backed Python project published by
``create_workflow`` or ``revise_workflow``. This package marks the canonical
catalog location; the projects themselves are discovered from the filesystem
rather than imported as children of this module.
"""
